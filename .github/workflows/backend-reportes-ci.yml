name: Backend CI - Reportes

on:
  push:
    branches: [ develop, main ]
    paths:
      - 'Backend-Vigia/MicroservicioReportes/**'
  pull_request:
    branches: [ develop, main ]
    paths:
      - 'Backend-Vigia/MicroservicioReportes/**'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout del código
        uses: actions/checkout@v4

      - name: Configurar JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'maven'

      - name: Compilar y empaquetar con Maven
        run: mvn -f Backend-Vigia/MicroservicioReportes/pom.xml clean package -DskipTests

      - name: Verificar artefacto generado
        run: ls -l Backend-Vigia/MicroservicioReportes/target/*.jar