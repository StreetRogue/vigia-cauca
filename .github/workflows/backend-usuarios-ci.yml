name: Backend CI - Usuarios

on:
  push:
    branches: [ develop, main ]
    paths:
      - 'Backend-Vigia/micro-usuarios/**'
  pull_request:
    branches: [ develop, main ]
    paths:
      - 'Backend-Vigia/micro-usuarios/**'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout del código
        uses: actions/checkout@v4

      - name: Configurar JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'maven'

      - name: Compilar y empaquetar con Maven
        run: mvn -f Backend-Vigia/micro-usuarios/pom.xml clean package -DskipTests

      - name: Verificar artefacto generado
        run: ls -l Backend-Vigia/micro-usuarios/target/*.jar