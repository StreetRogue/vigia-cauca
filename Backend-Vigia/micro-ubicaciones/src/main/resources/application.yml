server:
    port: ${APP_PORT:8082}

spring:
    application:
        name: micro-ubicaciones
    profiles:
        active: ${SPRING_PROFILES_ACTIVE:dev}

logging:
    level:
        org.springframework: INFO